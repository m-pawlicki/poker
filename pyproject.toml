[project]
name = "poker"
version = "0.1.0"
description = "Planning Poker application built using Flask"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "nanoid>=2.0.0",
    "python-dotenv>=1.2.2",
]
